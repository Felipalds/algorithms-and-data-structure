package main

import "math"

func main() {

}

func gcdOfStrings(str1 string, str2 string) string {

	l1 := len(str1)
	l2 := len(str2)

	max := int(math.Max(float64(l1), float64(l2)))

	var gdc []byte
	for i := 0; i < max; i++ {
		if l1 > l2 {
				if

		} else {

		}
	}
}
